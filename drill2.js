// 2. Even or odd

// What is the Big O of the following algorithm? Explain your answer

function isEven(value) {
  if (value % 2 == 0) {
    return true;
  } else {
    return false;
  }
}

// * O(1) - Because it is constant and does not change the number of operations run regardless of input.
